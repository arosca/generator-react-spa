/** @jsx React.DOM */
'use strict';

var React = require('react');

var Footer = React.createClass({
    render: function() {
        return (
            <footer>
            	<p>♥ from the Yeoman team</p>
            </footer>
        );
    }
});

module.exports = Footer;
